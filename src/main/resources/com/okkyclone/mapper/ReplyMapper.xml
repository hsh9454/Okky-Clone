<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.okkyclone.mapper.ReplyMapper">
 
    <insert id="insert">
        insert into tbl_reply (rno, bno, reply, replyer)
        values (seq_reply.nextval, #{bno}, #{reply}, #{replyer})
    </insert>

    <select id="getList" resultType="com.okkyclone.domain.ReplyVO">
        select * from tbl_reply where bno = #{bno} order by rno asc
    </select>

    <delete id="delete">
        delete from tbl_reply where rno = #{rno}
    </delete>
   
    <select id="getListWithPaging" resultType="com.okkyclone.domain.ReplyVO">
        <![CDATA[
          SELECT rno, bno, reply, replyer, replyDate, updateDate
          FROM tbl_reply
          WHERE bno = #{bno}
         ORDER BY rno ASC
        ]]>
     </select>
     
    <update id="update">
        update tbl_reply set reply = #{reply}, updateDate = sysdate
        where rno = #{rno}
    </update>
     
</mapper>